# HCS-01 Policy & Leave Concierge — Python Dependencies

# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Vector Database
qdrant-client>=1.9.0

# LLM & Embeddings
openai>=1.40.0
litellm>=1.44.0
tiktoken>=0.7.0

# Rendering the policy PDFs from their Markdown sources. Arabic needs all three:
# an embedded font, letter joining, and right-to-left reordering. Without them
# ReportLab silently draws a dingbat for every Arabic character.
reportlab>=4.0.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# LangGraph (Agent Orchestration)
langgraph>=0.2.0
# Saves conversation state, so a clarification can pause and resume across requests
# and a conversation remembers what was said in it
langgraph-checkpoint-sqlite>=2.0.0

# Database ORM
sqlalchemy>=2.0.0

# Retry Logic
tenacity>=8.2.0

# PDF Parsing
pymupdf>=1.24.0

# Streamlit UI
streamlit>=1.36.0

# HTTP Client (for UI → API calls)
httpx>=0.27.0

# Data & Config
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# File parsing
python-multipart>=0.0.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
